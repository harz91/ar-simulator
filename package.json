{
  "name": "ar-simulator-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "server"
  ],
  "scripts": {
    "dev:studio": "npm run dev --workspace=apps/studio",
    "dev:trainer": "npm run dev --workspace=apps/trainer",
    "dev:server": "npm run dev --workspace=server"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}